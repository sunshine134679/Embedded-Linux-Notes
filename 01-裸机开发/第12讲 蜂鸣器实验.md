# 第12讲 蜂鸣器实验

> 来源：正点原子官方笔记整理

## 一、硬件原理图分析

蜂鸣器控制 IO 为 `SNVS_TAMPER1`。

| 电平 | 蜂鸣器状态 |
| ---- | ---- |
| 高电平 | 关闭 |
| 低电平 | 打开 |

## 二、实验程序编写

程序流程：

1. 初始化 `SNVS_TAMPER1`，复用为 `GPIO5_IO01`。
2. 设置 `SNVS_TAMPER1` 的电气属性。
3. 初始化 GPIO。
4. 控制 GPIO 输出高 / 低电平。
